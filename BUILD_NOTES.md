# Build notes — Ghostpanter/scrcpy-android

## Status (2026-09-18 Asia/Shanghai)
**NOT yet rebuildable.** Partial source tree on `main`; continue MCP `push_files` / `create_or_update_file` from `/workspace/scrcpy-android-build`.

### Landed (this session + prior)
- Most `app/src/main/res/` (drawables, values, mirror/qr_pair/main layouts, mipmap, xml)
- Helper Java: App, AtomicFiles, AudioFrames, Log, Ui, VideoFrames, TouchGeometry/Map, Licenses, VideoQueue, Crashlog, Sessions, AudioStream, QrCodes, Sync, ControlMessages
- Scripts: update-server, check, build-server, check-server, check-server-source, check-wrapper, build-apk
- `:adb` partial: libadb/build.gradle, AndroidManifest, KeyPair, AdbInputStream, AdbOutputStream
- Gradle wrapper properties + `.sha256` (jar still omitted)

### Still missing for assembleDebug
**App Java**
- Adb.java, AudioSink.java, ControlStream.java, Controller.java, Devices.java, Main.java, Mirror.java, QrPair.java, Server.java, Session.java, Settings.java, SettingsActivity.java, VideoSink.java, VideoStream.java, Wire.java

**Layouts / root**
- `app/src/main/res/layout/settings.xml`
- `LICENSE`, `gradlew`
- `gradle/wrapper/gradle-wrapper.jar` — restore: `gradle wrapper --gradle-version 8.13`

**vendor/libadb-android `:adb` (required)**
- AdbConnection.java, AdbProtocol.java, AdbStream.java, AndroidPubkey.java, PairingAuthCtx.java, PairingConnectionCtx.java, SslUtils.java
- Plus COPYING/README/LICENSES (compliance)

**Optional**
- app/src/debug/**, app/src/test/**, remaining fastlane text, test-rig extras, gradle/verification-metadata.xml

## Rebuild (after gaps filled)
```bash
git clone https://github.com/Ghostpanter/scrcpy-android.git
cd scrcpy-android
./scripts/update-server
# If gradle-wrapper.jar missing:
#   gradle wrapper --gradle-version 8.13
./gradlew :app:assembleDebug
```

## Requirements
- JDK 17; Android SDK per `app/build.gradle`

## Intentionally omitted
| Path | Why |
|------|-----|
| `app/src/main/assets/scrcpy-server.jar` | Regenerated by `scripts/update-server` |
| `local.properties`, `.gradle/`, `build/`, `artifacts/`, `*.apk` | Local / caches / outputs |
| `vendor/scrcpy/**` | Submodule; not needed after `update-server` |
| Fastlane PNGs | Store listing |
| `gradle/wrapper/gradle-wrapper.jar` | Binary; restore via `gradle wrapper` |

## Package
`com.ghostpanter.scrcpy` (`0.5-ghostpanter`)
